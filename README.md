# Sonara Landing

Luxury diamond jewelry landing page (OYLA-style scroll architecture).

```bash
cd sonara-landing
npm install
npm run dev
```

Open http://localhost:3000

```bash
npm run build && npm start
```

Hero/reveal videos are the original CloudFront cinematic clips in `index.html` (`#hero-video` / `#reveal-video`).
